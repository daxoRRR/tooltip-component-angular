export enum placementTooltip {
  top = "top",
  bottom = "bottom",
  left = "left",
  right = "right"
}

export interface positionTooltip {
  top: number;
  left: number;
  width?: number;
  height?: number;
}
